<Window
  x:Class="YXSTool.View.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
  xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
  xmlns:local="clr-namespace:YXSTool.View"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
  xmlns:vm="clr-namespace:YXSTool.ViewModel"
  Title="YXSTool"
  Width="1024"
  Height="720"
  MinWidth="1024"
  MinHeight="720"
  d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
  ui:TitleBar.Height="0"
  ui:WindowHelper.SystemBackdropType="Mica"
  ui:WindowHelper.UseModernWindowStyle="True"
  WindowStartupLocation="CenterScreen"
  WindowStyle="None"
  mc:Ignorable="d">
  <Window.Resources>
    <Style
      x:Key="CustomTabControlStyle"
      BasedOn="{StaticResource DefaultTabControlStyle}"
      TargetType="TabControl">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TabControl">
            <Grid ClipToBounds="True" KeyboardNavigation.TabNavigation="Local">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>

              <!--  标签栏容器  -->
              <Grid
                Grid.Row="0"
                Margin="3,3,0,0"
                Background="{TemplateBinding Background}">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ikw:SimpleStackPanel
                  Grid.Column="0"
                  Background="Transparent"
                  Orientation="Horizontal">
                  <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseDown">
                      <i:InvokeCommandAction Command="{Binding DataContext.MoveAndDoubleClickCommand, RelativeSource={RelativeSource AncestorType=TabControl}}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                  </i:Interaction.Triggers>

                  <!--  TabPanel - 显示所有标签  -->
                  <TabPanel
                    VerticalAlignment="Center"
                    Panel.ZIndex="1"
                    IsItemsHost="True"
                    KeyboardNavigation.TabIndex="1"
                    KeyboardNavigation.TabNavigation="Local" />

                  <!--  Add 按钮 - 紧挨着标签  -->
                  <Menu Margin="-4,-4,0,-4">
                    <MenuItem ItemsSource="{Binding DataContext.TabItemInfos, RelativeSource={RelativeSource AncestorType=TabControl}}">
                      <i:Interaction.Triggers>
                        <i:EventTrigger EventName="PreviewMouseDown">
                          <i:InvokeCommandAction Command="{Binding DataContext.AddTabItemCommand, RelativeSource={RelativeSource AncestorType=TabControl}}" PassEventArgsToCommand="True" />
                        </i:EventTrigger>
                      </i:Interaction.Triggers>
                      <MenuItem.Header>
                        <ui:FontIcon FontSize="14" Icon="{x:Static ui:SegoeFluentIcons.Add}" />
                      </MenuItem.Header>
                      <MenuItem.ItemTemplate>
                        <DataTemplate>
                          <StackPanel Orientation="Horizontal">
                            <ui:FontIcon Foreground="MediumPurple" Icon="{Binding Icon}" />
                            <TextBlock Margin="8,0,0,0" Text="{Binding Header}" />
                          </StackPanel>
                        </DataTemplate>
                      </MenuItem.ItemTemplate>
                      <MenuItem.ItemContainerStyle>
                        <Style BasedOn="{StaticResource DefaultMenuItemStyle}" TargetType="MenuItem">
                          <Setter Property="Command" Value="{Binding DataContext.AddTabItemCommand, RelativeSource={RelativeSource AncestorType=TabControl}}" />
                          <Setter Property="CommandParameter" Value="{Binding Header}" />
                        </Style>
                      </MenuItem.ItemContainerStyle>
                    </MenuItem>
                  </Menu>
                </ikw:SimpleStackPanel>

                <!--  右侧按钮容器  -->
                <StackPanel
                  Grid.Column="1"
                  HorizontalAlignment="Right"
                  Orientation="Horizontal">
                  <ui:TitleBarButton Command="{Binding MinimizeCommand}">
                    <ui:TitleBarButton.Content>
                      <ui:FontIcon FontSize="12" Icon="{x:Static ui:SegoeFluentIcons.ChromeMinimize}" />
                    </ui:TitleBarButton.Content>
                  </ui:TitleBarButton>
                  <ui:TitleBarButton Command="{Binding CloseCommand}">
                    <ui:TitleBarButton.Content>
                      <ui:FontIcon FontSize="12" Icon="{x:Static ui:SegoeFluentIcons.ChromeClose}" />
                    </ui:TitleBarButton.Content>
                  </ui:TitleBarButton>
                </StackPanel>
              </Grid>

              <!--  内容区域  -->
              <ContentPresenter
                Name="PART_SelectedContentHost"
                Grid.Row="1"
                Margin="{TemplateBinding Padding}"
                ContentSource="SelectedContent" />
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style
      x:Key="CloseTabItemStyle"
      BasedOn="{StaticResource DefaultTabItemStyle}"
      TargetType="TabItem">
      <Setter Property="ui:TabItemHelper.IsClosable" Value="False" />
      <Setter Property="HeaderTemplate">
        <Setter.Value>
          <DataTemplate>
            <Border Margin="0">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Margin="0,0,6,0">
                  <ui:FontIcon
                    FontSize="17"
                    Foreground="MediumPurple"
                    Icon="{Binding DataContext.Icon, RelativeSource={RelativeSource AncestorType=TabItem}}" />
                </Border>
                <TextBlock
                  Grid.Column="1"
                  VerticalAlignment="Center"
                  FontSize="14"
                  Text="{Binding DataContext.Header, RelativeSource={RelativeSource AncestorType=TabItem}}" />
                <Button
                  Grid.Column="2"
                  Margin="6,0,0,0"
                  Padding="4"
                  VerticalAlignment="Center"
                  Background="Transparent"
                  BorderThickness="0"
                  Command="{Binding DataContext.RemoveTabItemCommand, RelativeSource={RelativeSource AncestorType=TabControl}}"
                  CommandParameter="{Binding DataContext.Header, RelativeSource={RelativeSource AncestorType=TabItem}}"
                  Foreground="IndianRed">
                  <Button.Content>
                    <ui:FontIcon FontSize="10" Icon="{x:Static ui:SegoeFluentIcons.ChromeClose}" />
                  </Button.Content>
                </Button>
              </Grid>
            </Border>
          </DataTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <TabControl
    ItemContainerStyle="{StaticResource CloseTabItemStyle}"
    ItemsSource="{Binding TabItems}"
    SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}"
    Style="{StaticResource CustomTabControlStyle}">
    <TabControl.ContentTemplate>
      <DataTemplate>
        <ContentControl Content="{Binding Content}" />
      </DataTemplate>
    </TabControl.ContentTemplate>
  </TabControl>
</Window>
